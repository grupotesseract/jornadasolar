import { createAction } from '@reduxjs/toolkit'

export const createdOrUpdated = createAction('novidades/createdOrUpdated')
export const deleteNovidade = createAction('novidades/delete')

export default {}
