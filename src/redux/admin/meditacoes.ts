import { createAction } from '@reduxjs/toolkit'

export const createdOrUpdated = createAction('meditacoes/createdOrUpdated')
export const deleteMeditacao = createAction('meditacoes/delete')

export default {}
