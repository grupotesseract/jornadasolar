import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#FFFFFF'
    },
    secondary: {
      main: '#F7C92A'
    },
    background: {
      default: '#000000'
    }
  }
})

export default theme
